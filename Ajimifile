# Ajimi configuration file

# source setting
source_host "sandbox-app03b"
source_user "morita"
source_key "~/.ssh/id_rsa"

# target setting
target_host "sandbox-webapp"
target_user "ec2-user"
target_key "~/.ssh/MasayukiMORITA.pem"

# check setting
check_root_path "/root"

ignore_paths [
  "/root/.bash_history",
  %r|\A/root/\.gem/?.*|,
  "/root/.lesshst"
]

ignore_contents ({
  "/root/.ssh/authorized_keys" => /Please login as the user/
})

pending_paths [
  "/root/du.log"
]

pending_contents ({
  "/root/.viminfo" => /.*/
})
